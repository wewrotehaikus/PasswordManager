#include "LoginWindow.h"
#include "Vault.h"

LoginWindow::LoginWindow() : QMainWindow() {
    //Create UI elements
    //Connect button clicks to your slots
}

void LoginWindow::onLoginButtonClicked() {
    // Get password form text field
    // Call vault.unlock(password)
    // Show main window if successful
}